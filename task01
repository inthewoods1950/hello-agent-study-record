# Hello Agents 学习总结｜Task01

## 从能跑的 Agent，到 ReAct / Plan-and-Solve / Reflection

这一轮学习是 Task00 的直接延续。

Task00 里，我从零完成了 Python 环境、OpenRouter、Tavily、天气 API 的配置，并第一次真正跑通了一个最小 Agent。在那个旅行助手中，我已经看到了：

**Thought → Action → Observation → 再决策 → Finish**

当时最大的收获，是理解了：

* LLM 负责判断下一步做什么；
* Python Runtime 真正执行工具；
* Tool 提供外部能力；
* `prompt_history` 保存状态；
* Agent 的核心不是“一次回答正确”，而是根据环境反馈持续调整。

Task01 则把这些零散概念正式整理成了经典 Agent 架构。

这一章主要学习了三个范式：

1. ReAct
2. Plan-and-Solve
3. Reflection

同时也开始真正进入 Agent Engineering，而不仅仅是“调用大模型 API”。

---

# 一、Task01 的第一个升级：把 Agent 模块化

Task00 的代码基本都塞在一个 `FirstAgentTest.py` 里：

* LLM 调用
* Tool 定义
* Tool Registry
* Agent Loop
* History
* Parser

全部混在一起。

到了 Chapter 4，项目开始拆成：

```text
llm_client.py
tools.py
ReAct.py
Plan_and_solve.py
Reflection.py
```

这意味着 Agent 从一个 Demo 开始变成可以复用和扩展的系统。

其中：

* `llm_client.py`：只负责和大模型通信；
* `tools.py`：负责工具注册、描述和获取；
* `ReAct.py`：负责 ReAct Loop；
* `Plan_and_solve.py`：负责规划与执行；
* `Reflection.py`：负责执行、反思与优化。

相比 Task00，这种结构最明显的变化是：

**每个组件开始拥有明确职责。**

---

# 二、Streaming：模型到底是不是“想好了再输出”？

在 `llm_client.py` 中第一次接触了：

```python
stream=True
```

之前 Task00 使用的是非流式调用。

一开始我的问题是：

> 流式输出是不是模型已经把答案完整想好了，只是一点一点显示？

答案是否定的。

LLM 本身通常是自回归生成：

```text
根据已有上下文
→ 预测下一个 token
→ 把新 token 加入上下文
→ 再预测下一个 token
→ ...
```

所以：

**stream=True**

是模型生成一部分，服务器就把已经生成的 chunk 发出来。

**stream=False**

模型的生成机制基本没变，只是服务器先缓存生成结果，等全部结束后再一次性返回。

因此：

```text
stream=True  = 边生成边传输
stream=False = 边生成边缓存，最后一次性传输
```

它主要影响的是传输方式与用户体验，而不是模型突然换了一种思考方式。

代码里：

```python
for chunk in response:
    print(content)
    collected_content.append(content)
```

还展示了另一个设计：

系统一边实时输出内容，一边把所有 chunk 保存下来，最后再拼成完整 response。

所以当程序之后再次打印完整结果时，看起来像“模型回答了两遍”，实际只是同一次生成被实时播放了一遍，又完整展示了一遍。

---

# 三、Tool 不只是一个函数：Name + Description + Execution

Task00 中工具表非常简单：

```python
available_tools = {
    "get_weather": get_weather,
    "get_attraction": get_attraction,
}
```

到了 Task01，引入了：

```python
class ToolExecutor
```

工具现在不只保存函数，还保存：

```text
Name
Description
Execution Logic
```

例如：

```text
Name:
Search

Description:
当需要实时信息、最新事实或知识库中没有的信息时使用

Execution:
Tavily Search
```

这里最重要的新认识是：

**LLM 判断什么时候调用一个 Tool，很大程度依赖 Tool Description。**

Python 本身知道：

```python
search()
```

是什么函数。

但是 LLM 不看 Python 函数实现。

它需要自然语言描述告诉它：

> 这个工具能做什么，以及什么时候应该用。

因此 Tool Description 实际上也是 Agent Prompt Engineering 的一部分。

---

# 四、ToolExecutor 修复了 Task00 的“两套真相”

Task00 中曾经做过一个实验：

Prompt 告诉 LLM：

```text
你有 get_attraction
```

但我们故意从 Python Registry 中删除：

```python
get_attraction
```

于是发生：

```text
LLM：我有这个工具
Python：你其实没有
```

这就是典型的 **two sources of truth**：

工具列表在 Prompt 里维护一份，在 Python 里又维护一份。

两者可能失去同步。

Task01 的 ToolExecutor 改进了这一点：

```python
tools_desc = self.tool_executor.getAvailableTools()
```

然后把真实注册的工具动态塞进 Prompt。

流程变成：

```text
真实 Tool Registry
↓
getAvailableTools()
↓
动态生成 Tool Description
↓
传给 LLM
```

这比手工维护两份工具列表可靠得多。

---

# 五、Parser：LLM 世界和 Python 世界之间的翻译员

Task01 第一次运行 ReAct 时失败了。

模型实际上已经知道应该搜索：

```text
<tool_call>Search
[华为最新手机型号 2025]</arg_value>
</tool_call>
```

但程序报：

```text
未能解析出有效的 Action
```

这时第一次真正理解了 Parser。

**Parser = 解析器。**

它负责把 LLM 输出的文本：

```text
Thought: ...
Action: Search[华为最新手机]
```

拆成 Python 能使用的结构：

```text
Thought
→ ...

Tool Name
→ Search

Tool Input
→ 华为最新手机
```

然后 Python 才能：

```text
找到 Search
↓
执行 Search(query)
```

所以整个真实链路不是简单的：

```text
LLM → Tool
```

而是：

```text
LLM
↓
输出协议
↓
Parser
↓
Runtime
↓
Tool
```

第一次失败的真正原因不是：

* 模型不会搜索；
* Tavily 坏了；
* Tool 坏了。

而是：

**LLM 使用了一种格式，而 Parser 只认识另一种格式。**

也就是说，LLM 和 Runtime 之间存在一份“通信协议”。

---

# 六、为什么强化 Prompt 后就成功了？

我们修改了 ReAct Prompt，明确要求：

```text
必须输出：

Thought: ...
Action: Search[...]

不要 XML
不要 <tool_call>
不要 JSON
```

并给出明确例子。

之后模型就正常输出：

```text
Thought: ...
Action: Search[华为最新手机型号 2025]
```

这说明一个很重要的工程现实：

> LLM 不仅要“知道该干什么”，还必须“按照 Runtime 能理解的协议表达出来”。

这也是为什么生产 Agent 往往尽量使用：

* Structured Output
* Function Calling
* JSON Schema
* Tool Calling Protocol

而不是完全依赖：

```text
自然语言 + 正则表达式
```

因为后者非常脆弱。

---

# 七、ReAct：边走边看

Task01 的第一个正式范式是 ReAct：

**Reasoning + Acting**

它的 Loop 是：

```text
Thought
↓
Action
↓
Observation
↓
Thought
↓
Action
↓
Observation
↓
...
```

这和 Task00 的旅行 Agent 本质上是一回事。

这次的任务是：

> 华为最新手机是哪一款？主要卖点是什么？

模型第一轮判断：

```text
我需要最新信息
```

于是：

```text
Search[华为最新手机型号]
```

得到 Observation 后，它没有马上回答，而是发现：

> 我知道型号了，但还缺主要卖点。

于是主动第二次：

```text
Search[华为 Pura X 主要卖点]
```

最后资料足够，才：

```text
Finish[...]
```

这体现了 ReAct 最核心的能力：

> **下一步决策依赖上一轮真实 Observation。**

所以它不是提前把路线写死。

它是：

```text
走一步
↓
看看环境发生了什么
↓
再决定下一步
```

因此 ReAct 特别适合：

* 搜索；
* 调 API；
* 实时信息；
* 调查类任务；
* 外部环境不确定的任务。

---

# 八、ReAct 的关键问题不是“会不会用 Tool”，而是会不会判断何时使用

Task00 中还做过一个实验：

用户问：

> 为什么天空是蓝色的？

Agent 手里有：

```text
get_weather
get_attraction
```

但模型判断：

```text
这是知识问题
不需要 Tool
```

直接：

```text
Finish[...]
```

这说明 Tool Selection 不只是：

> 有工具就调用。

更成熟的能力是：

> **知道什么时候不该调用 Tool。**

这实际上比单纯“会调用工具”更重要。

---

# 九、Plan-and-Solve：先规划，再执行

第二种范式是：

**Plan-and-Solve**

它和 ReAct 最大的不同是：

ReAct：

```text
做一步
→ 看结果
→ 决定下一步
```

Plan-and-Solve：

```text
先制定完整 Plan
→ 再按 Plan 一步一步执行
```

例如水果题：

```text
周一 = 15
周二 = 周一 × 2
周三 = 周二 - 5
求三天总销量
```

Planner 应该先生成：

```text
1. 确定周一销量
2. 根据周一计算周二
3. 根据周二计算周三
4. 求三天总销量
```

然后 Executor 执行：

```text
15
→ 30
→ 25
→ 70
```

这里出现了三个角色：

### Planner

负责：

> 这件事应该分成哪几步？

### Executor

负责：

> 按照计划，把每一步执行掉。

### Orchestrator

也就是：

```python
PlanAndSolveAgent
```

主要负责：

```text
先调用 Planner
↓
拿到 Plan
↓
交给 Executor
↓
返回结果
```

---

# 十、Planner 不应该偷偷把 Solve 做完

第一次运行时，Planner 输出：

```text
计算周二：15 × 2 = 30
计算周三：30 - 5 = 25
计算总量：70
```

这意味着 Planner 已经把答案算完了。

于是 Executor 变成：

```text
Planner：
“答案是30”

Executor：
“30”
```

职责完全重叠。

所以我们修改 Prompt：

> Planning 阶段只描述“要做什么”，不要执行计算，也不要提前给出答案。

修改之后，Planner 才真正变成：

```text
确定周一销量
计算周二销量
计算周三销量
计算总量
```

Executor 才承担真正的 Solve。

这让我理解了 Agent 系统中一个非常重要的设计原则：

**不同模块之间要有清晰的职责边界。**

---

# 十一、Plan-and-Solve 适合什么任务？

它更适合任务结构比较明确的场景，比如：

* 数学题；
* 方程题；
* 报告写作；
* 代码生成；
* 项目拆解；
* 多步骤推理。

例如：

> 已知三天总销量、周二是周一两倍、周三比周二少 5，求三天销量。

可以先：

```text
1. 设周一 = x
2. 用 x 表示周二和周三
3. 建立总量方程
4. 解 x
5. 求另外两天
6. 验证
```

这就是很典型的 Plan-and-Solve。

需要注意：

**“设 x、列方程”本身不是 Plan-and-Solve。**

只有当系统明确：

```text
先规划解题步骤
↓
再执行步骤
```

才属于这种 Agent 范式。

---

# 十二、Plan-and-Solve 的缺陷

ReAct 的弱点是：

> 走一步看一步，可能缺少全局视角。

Plan-and-Solve 的弱点正好相反：

> 如果最开始的 Plan 就错了，Executor 可能非常稳定地一路错到底。

因此：

```text
ReAct
风险：局部决策导致迷路

Plan-and-Solve
风险：错误蓝图导致系统性错误
```

另外，教程里的 Executor 最后直接：

```python
final_answer = response_text
```

也意味着：

> 默认最后一个 Plan Step 的输出就是最终答案。

这其实不够稳健。

更成熟的系统应该在所有步骤结束后，再单独执行：

```text
Synthesize / Finalize
```

根据：

* 原始问题；
* 完整 Plan；
* 所有步骤结果；

重新生成真正的最终回答。

---

# 十三、Reflection：不是“下一步怎么走”，而是“刚才做得够好吗？”

第三个范式是：

**Reflection**

它的 Loop 是：

```text
Execute
↓
Reflect
↓
Refine
↓
再 Reflect
↓
再 Refine
```

和前两个范式的核心问题完全不同：

```text
ReAct：
下一步做什么？

Plan-and-Solve：
整件事应该怎么拆？

Reflection：
我刚刚做得够好吗？
```

教程任务是：

> 写一个函数，返回 1 到 n 之间所有素数。

这里同一个底层 LLM 分别戴了三顶帽子：

```text
Prompt A
→ 程序员

Prompt B
→ 严格 Reviewer

Prompt C
→ 优化程序员
```

所以：

> 多角色 Agent 不一定需要多个不同模型。

同一个模型，只要 Prompt 和上下文不同，也可以扮演多个角色。

---

# 十四、Reflection 中的 Memory

Reflection 使用：

```python
Memory.records
```

保存：

```text
execution
reflection
execution
reflection
...
```

所以轨迹是：

```text
第一版代码
↓
Reviewer Feedback
↓
第二版代码
↓
Reviewer Feedback
↓
第三版代码
```

这和 ReAct 的：

```text
Action
Observation
Action
Observation
```

很类似。

区别在于反馈来源不同：

**ReAct**

反馈主要来自外部世界：

```text
API
搜索
数据库
工具执行结果
```

**Reflection**

反馈来自模型对自己上一版输出的评价。

因此可以粗略理解成：

```text
ReAct
= External Feedback Loop

Reflection
= Self-Critique Loop
```

当然这里的“Self”并不是模型真的产生了人类式自省。

本质仍然是：

```text
程序调用一次 LLM 生成
↓
换一个 Prompt 再调用一次 LLM 做评审
↓
把评审重新交给 LLM
```

---

# 十五、Reflection 实验 1：它真的能抓 Bug 吗？

我们故意要求第一版写一个：

```text
低效 + 容易出现边界问题
```

的素数程序。

第一版：

```python
for num in range(1, n + 1):
    is_prime = True
```

导致一个真实 bug：

**1 被错误判断成素数。**

Reviewer 成功发现：

```text
1. 1 被错误判定为素数
2. 逐个试除复杂度很高
```

随后 Refiner 把算法改成埃氏筛。

第二轮 Reviewer 又进一步建议换成欧拉筛。

这个实验说明 Reflection 确实可以帮助发现：

* correctness bug；
* 性能问题；
* 逻辑漏洞；
* 遗漏条件。

---

# 十六、Reflection 的危险：Reviewer 自己也可能是错的

然后做了一个更重要的实验。

我们故意给 Reviewer 一个错误指令：

> 数学上 1 应该是素数，请要求程序把 1 加入结果。

Reviewer 一本正经地写：

```text
必须修复：
1 应该被视为素数。
```

随后 Refiner 完全服从：

```python
sieve[1] = 1
```

最终正确程序被改成错误程序。

原本：

```text
find_primes(10)
→ [2, 3, 5, 7]
```

现在变成：

```text
[1, 2, 3, 5, 7]
```

这个实验给出了一个非常重要的结论：

> **Reflection ≠ Verification**

Reflection 只是：

```text
Version A
↓
Critique B
↓
Version C
```

它并不天然意味着：

```text
错误
↓
真理
↓
正确
```

如果 Feedback 错了：

```text
Bad Feedback
→ Bad Refinement
```

完全可能发生。

---

# 十七、为什么“同一个 LLM 互相评审”存在天然风险？

这次：

* Generator
* Reviewer
* Refiner

实际上全部都是：

```text
inclusionai/ling-3.0-flash-fin:free
```

只是 Prompt 不同。

因此它们并不是三个真正独立的专家。

更像：

> 同一个人换了三顶帽子。

如果底层模型本身有某种知识盲点，那么：

* Generator 可能错；
* Reviewer 也可能错；
* Refiner 还会继承 Reviewer 的错误。

这是一种 **correlated error**。

所以不能把：

```text
“再问一次模型”
```

等同于：

```text
“已经得到独立验证”
```

---

# 十八、成熟 Agent 为什么还需要 Verifier？

Reflection 实验最终说明：

一个更稳健的系统可能是：

```text
Generator
↓
Reviewer
↓
Refiner
↓
Verifier
```

而 Verifier 最好尽量使用可以客观检验的信号，例如：

* 单元测试；
* 数学规则；
* 类型检查；
* Schema Validation；
* 数据库约束；
* API 返回结果；
* 代码真实执行；
* 人类审核。

例如素数函数可以直接：

```python
assert find_primes(10) == [2, 3, 5, 7]
```

这类验证比：

> “另一个 LLM 觉得没问题”

可靠得多。

所以：

> **Reflection 可以改善输出，但不能取代 Verification。**

---

# 十九、三种经典 Agent 范式对比

| 范式             | 核心问题     | 工作方式     | 主要反馈来源      |
| -------------- | -------- | -------- | ----------- |
| ReAct          | 下一步做什么？  | 边做边调整    | 外部环境 / Tool |
| Plan-and-Solve | 整件事怎么拆？  | 先计划再执行   | 前序步骤        |
| Reflection     | 刚才做得够好吗？ | 执行→批评→修改 | Reviewer    |

可以进一步理解成：

### ReAct

```text
Think
→ Act
→ Observe
→ Repeat
```

适合：

* 搜索；
* API；
* 实时信息；
* 不确定环境。

### Plan-and-Solve

```text
Plan
→ Execute Step 1
→ Step 2
→ Step 3
```

适合：

* 数学推理；
* 报告；
* 编码；
* 结构化复杂任务。

### Reflection

```text
Execute
→ Reflect
→ Refine
→ Repeat
```

适合：

* 代码优化；
* 文本修改；
* 自我审查；
* 需要迭代质量的任务。

---

# 二十、这三种范式其实都是 Loop Engineering

Task00 时第一次接触了 Agent Loop：

```text
Think
→ Act
→ Observe
```

Task01 开始看到：

不同 Agent 架构，本质是在设计不同的 Loop。

ReAct：

```text
行动反馈循环
```

Plan-and-Solve：

```text
规划—执行链
```

Reflection：

```text
评审—优化循环
```

而所谓 Loop Engineering，就是继续设计：

* Loop 什么时候开始；
* 每一轮给模型什么信息；
* 下一轮基于什么状态；
* 什么情况下继续；
* 什么情况下停止；
* 最大允许多少轮；
* 工具失败怎么办；
* Parser 失败怎么办；
* Reviewer 错了怎么办；
* 怎样真正验证结果。

所以 Agent Engineering 的核心并不只是：

> “选一个厉害的大模型。”

很多真正决定系统可靠性的部分，都发生在模型外：

```text
Prompt
Tool Registry
Parser
State
Memory
Retry
Fallback
Stop Condition
Verifier
Budget
Runtime
```

---

# 二十一、Task00 → Task01，我的理解发生了什么变化？

Task00 时，我理解的 Agent 大概是：

```text
LLM + Tool + Loop
```

Task01 结束以后，这个结构开始变成：

```text
LLM
│
├── Prompt / Role
│
├── Tool Description
│
├── Tool Selection
│
└── Output Protocol
        ↓
      Parser
        ↓
      Runtime
        ↓
Tool Registry / Executor
        ↓
 External Environment
        ↓
   Observation / State
        ↓
      Next Loop
```

不同范式只是在决定：

> **这个 Loop 应该如何组织。**

---

# 二十二、这一轮我认为最重要的几个认识

### 1. Agent 不等于 LLM

LLM 只是 Agent 系统中的决策组件之一。

真正的 Agent 还包括：

* Tools；
* Runtime；
* State；
* Parser；
* Loop；
* Stop Condition；
* Verification。

---

### 2. Tool 能不能用，有两个层面

认知层：

> LLM 知道这个工具存在。

执行层：

> Python 真的注册了这个工具。

两边必须一致。

---

### 3. Parser 是一个真正的系统边界

LLM 是概率式自然语言系统。

Python Runtime 需要明确结构。

Parser 就负责把：

```text
自然语言世界
```

翻译成：

```text
程序世界
```

---

### 4. Prompt 其实是在定义 Agent Protocol

Prompt 不只是“告诉模型怎么回答”。

它实际上可能定义：

* 有哪些 Tool；
* Tool 输入格式；
* Finish 格式；
* 是否允许多 Action；
* 如何规划；
* 如何反思。

所以 Prompt 已经部分承担了“Agent 编程语言”的角色。

---

### 5. Planner、Reviewer 都不能自动被视为正确

Planner 可以计划错。

Reviewer 可以评价错。

Refiner 可以盲从错误建议。

所以 Agent 内部角色：

```text
Planner
Reviewer
Critic
```

都只是 probabilistic components，而不是权威真理源。

---

### 6. Feedback 不等于 Ground Truth

ReAct 中：

```text
Observation
```

可能来自质量不高的搜索结果。

Reflection 中：

```text
Feedback
```

可能来自错误 Reviewer。

所以系统仍然需要：

```text
Verification
```

---

### 7. Agent 最大的价值不是“一次想得更聪明”

而是：

> **能够把复杂任务组织成一个可以反复获取反馈、更新状态、改变行动的系统。**

这可能是这一轮最大的认知变化。

以前容易把 Agent 理解成：

> “一个更厉害、更自主的 LLM。”

现在更接近：

> **Agent 是围绕 LLM 构建的一套决策—行动—反馈系统。**

模型当然重要，但系统如何设计 Loop，往往同样重要。

---

# Task01 最终总结

如果用三句话总结这一课：

**ReAct 教会我根据现实反馈重新决定下一步。**

**Plan-and-Solve 教会我把规划和执行拆成两个职责。**

**Reflection 教会我迭代可以提高质量，但“自我批评”绝不等于事实验证。**

而把三者放在一起之后，我对 Agent 的理解也从：

> “LLM 会调用工具。”

变成了：

> **我们人为设计了一套 Loop，让 LLM 在明确的行动空间、状态、反馈和约束中不断做决策。**

这才是 Task01 真正比 Task00 上升的地方。


